# Production API targets. Single-app deploy: the frontend is served from the
# same Fly app, so these point at that app's own domain (same origin = no CORS).
# Keep the host in sync with `app = ...` in fly.toml.
VITE_API_URL=https://ais-tracker.fly.dev
VITE_WS_URL=wss://ais-tracker.fly.dev/ws
