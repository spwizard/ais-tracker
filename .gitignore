# Python
backend/.venv/
**/__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/

# Env / secrets
**/.env
*.local
# …but keep the non-secret production target config (only public URLs)
!frontend/.env.production

# Data — generated + licensed (Lloyd's ownership.sqlite); never commit
backend/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Fly
.fly/

# OS
.DS_Store
